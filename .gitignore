# Dependencies
node_modules/
.pnp/
.pnp.js
.yarn/

# Builds / output
.next/
out/
build/
dist/
*.tsbuildinfo

# Environment / secrets
.env
.env.*
!.env.example
# Credential reference for the seeded dev logins. The passwords are already in
# prisma/seed.ts, but a doc that collects them is a far easier thing to find.
docs/dev-logins.md

# Database / storage (local dev)
*.sqlite
*.db
/data/
/minio-data/
/storage/

# Business data source files (real lab/inventory/personnel data — keep out of git)
*.xlsx
*.xls
*.csv

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Coverage / test
coverage/
.nyc_output/

# Pre-existing Claude-skills marketplace template (unrelated to this LIMS repo)
.claude/
.claude-plugin/

# Local tooling artifacts (not project code)
.claude-ease/
*.vsix
